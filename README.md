# How to run
```sh
# assuming mvn is installed in PATH
mvn javafx:run
```

# Tools
install maven (mvn)
```sh
# for macs assuming homebrew is installed
brew install maven
```
download [here](https://maven.apache.org/download.cgi)
